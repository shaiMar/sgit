{
  "name": "sgit",
  "displayName": "SGit",
  "description": "Shows changed files tracked by git, similar to git status",
  "version": "0.1.0",
  "publisher": "sgit",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["SCM Providers"],
  "icon": "resources/sgit-icon.png",
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sgit-container",
          "title": "SGit – Changed Files",
          "icon": "resources/sgit-icon.svg"
        }
      ]
    },
    "views": {
      "sgit-container": [
        {
          "id": "sgitChangedFiles",
          "name": "Changed Files",
          "when": "true"
        }
      ]
    },
    "commands": [
      {
        "command": "sgit.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "sgit.openDiff",
        "title": "SGit: Open Diff",
        "icon": "$(diff)"
      },
      {
        "command": "sgit.openFile",
        "title": "SGit: Open File",
        "icon": "$(go-to-file)"
      },
      {
        "command": "sgit.handleFileClick",
        "title": "SGit: Handle Click"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "sgit.refresh",
          "when": "view == sgitChangedFiles",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "sgit.openDiff",
          "when": "view == sgitChangedFiles && viewItem == file",
          "group": "navigation@1"
        },
        {
          "command": "sgit.openFile",
          "when": "view == sgitChangedFiles && viewItem == file",
          "group": "navigation@2"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pack": "npx @vscode/vsce package --allow-missing-repository",
    "install-ext": "npm run pack && cursor --install-extension sgit-0.1.0.vsix"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
